[
    {
        "key": "CsvPath",
        "type": "input",
        "defaultValue": "C:\\HelloID\\blocklist.csv",
        "templateOptions": {
            "label": "CSV File Path",
            "placeholder": "C:\\HelloID\\blocklist.csv",
            "description": "The path to the CSV file to export the data to",
            "required": true
        }
    },
    {
        "key": "Delimiter",
        "type": "input",
        "defaultValue": ",",
        "templateOptions": {
            "label": "Delimiter",
            "placeholder": ",",
            "description": "The delimiter that separates the property values in the CSV file. The default is a comma (,)",
            "required": true
        }
    },
    {
        "key": "Encoding",
        "type": "input",
        "defaultValue": "utf8",
        "templateOptions": {
            "label": "Encoding",
            "placeholder": "utf8",
            "description": "The encoding for the imported CSV file. The default value is utf8",
            "required": true
        }
    },
    {
        "key": "RetentionPeriod",
        "type": "input",
        "defaultValue": "365",
        "templateOptions": {
            "label": "RetentionPeriod (days)",
            "placeholder": "365",
            "description": "Retention period in days. This is the amount of days the value should remain blocked after it is deleted. Use 999999 if no retention period applies.",
            "required": true
        }
    }
]